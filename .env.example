# Application
PORT=8800
NODE_ENV=development

# SQLite database paths (absolute paths recommended)
# Note: These databases are created by claude-flow MCP servers
# Point to your project's .swarm and .hive-mind directories
#
# IMPORTANT: Database paths are SWAPPED due to schema naming:
# - SWARM_DB_PATH → hive.db (contains swarms, agents, tasks, messages tables)
# - HIVE_DB_PATH → memory.db (contains patterns, memory_entries, task_trajectories tables)
SWARM_DB_PATH="/Users/tannguyen/Documents/ACCESSTRADE/pay-per-post/.hive-mind/hive.db"
HIVE_DB_PATH="/Users/tannguyen/Documents/ACCESSTRADE/pay-per-post/.swarm/memory.db"

# MCP Server endpoints (NOT USED - for reference only)
# Dashboard reads directly from SQLite databases instead of via HTTP
# MCP servers (claude-flow, ruv-swarm, flow-nexus) use stdio transport, not HTTP
# MCP_CLAUDE_FLOW_URL="http://localhost:3000/mcp/claude-flow"
# MCP_RUV_SWARM_URL="http://localhost:3000/mcp/ruv-swarm"
# MCP_FLOW_NEXUS_URL="http://localhost:3000/mcp/flow-nexus"

# Refresh intervals
POLLING_INTERVAL_MS=5000
REALTIME_ENABLED=true

# Dashboard settings
DASHBOARD_TITLE="Claude Code Monitoring"
DEFAULT_TIMEFRAME="24h"
MAX_HISTORY_DAYS=30
